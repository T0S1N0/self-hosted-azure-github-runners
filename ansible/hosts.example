# Ansible inventory for GitHub runner VMs.
# Copy to hosts and fill in the IPs from Terraform output:
#   terraform -chdir=../terraform output -raw runner_public_ips_list
#
# Or use the script: ./scripts/generate-inventory.sh
#
# Use the same SSH user and key that Terraform used (admin_username and ssh key).

[runners]
# runner-1 ansible_host=1.2.3.4 runner_name=runner-1
# runner-2 ansible_host=5.6.7.8 runner_name=runner-2
# runner-3 ansible_host=9.10.11.12 runner_name=runner-3

[runners:vars]
ansible_user=azureuser
# If your SSH key is not the default, set:
# ansible_ssh_private_key_file=~/.ssh/id_rsa
